# Dependencies (restored with `npm install`)
node_modules/

# Bundled portable toolchain (Node.js lives in tools/ — re-download or see README)
tools/

# Build output (rebuild with `npm run release`)
dist/
release/

# Test artifacts (regenerated by `npm run test:e2e`) — keep only the README screenshot
test-output/*
!test-output/dashboard.png

# Logs
*.log
npm-debug.log*

# OS junk
.DS_Store
Thumbs.db
Desktop.ini

# Environment / secrets (defense in depth)
.env
.env.*
*.pem
*.key
