# Dependencies (restored with `npm install`)
node_modules/

# Bundled portable toolchain (Node.js lives in tools/ — re-download or see README)
tools/

# Build output (rebuild with `npm run package`)
dist/

# Test artifacts (screenshots, exported files — regenerated by `npm run test:e2e`)
test-output/

# Logs
*.log
npm-debug.log*

# OS junk
.DS_Store
Thumbs.db
Desktop.ini
